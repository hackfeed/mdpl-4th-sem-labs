PUBLIC CRLF

; Main code segment.
CODESEG SEGMENT PARA PUBLIC 'CODE'
    ASSUME CS:CODESEG

CRLF PROC NEAR
    MOV AH, 2
    MOV DL, 13
    INT 21H
    MOV DL, 10
    INT 21H
    RET
CRLF ENDP

CODESEG ENDS
END